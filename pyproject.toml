[project]
name = "wikipedia-utils"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = ["click>=8.0", "tqdm>=4.0"]

[project.optional-dependencies]
dev = ["pytest>=8.0"]

[tool.pytest.ini_options]
testpaths = ["markdown2mediawiki/tests"]
